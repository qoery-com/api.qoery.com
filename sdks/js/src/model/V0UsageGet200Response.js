/**
 * Qoetry API
 * Qoetry API for dataset collection and management. Start dataset collection jobs, monitor their progress, and download results as CSV files.  ## Authentication  **All endpoints require API key authentication** (except `/health` and `/`).  You can provide your API key via: - Query parameter: `?api_key=your_api_key` - Header: `x-api-key: your_api_key` - Header: `Authorization: Bearer your_api_key`  ## Rate Limits  API calls are subject to monthly quotas based on your subscription plan. Quota information is available via the `/v0/usage` endpoint. 
 *
 * The version of the OpenAPI document: 0.5.0
 * Contact: samuel.tinnerholm@gmail.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import V0UsageGet200ResponseEndpointBreakdownInner from './V0UsageGet200ResponseEndpointBreakdownInner';

/**
 * The V0UsageGet200Response model module.
 * @module model/V0UsageGet200Response
 * @version 0.5.0
 */
class V0UsageGet200Response {
    /**
     * Constructs a new <code>V0UsageGet200Response</code>.
     * Complete usage statistics for a user
     * @alias module:model/V0UsageGet200Response
     * @param userId {String} User identifier
     * @param plan {module:model/V0UsageGet200Response.PlanEnum} Current subscription plan
     * @param totalUsage {Number} Total number of API calls across all endpoints
     * @param totalErrors {Number} Total number of errors across all endpoints
     * @param endpointBreakdown {Array.<module:model/V0UsageGet200ResponseEndpointBreakdownInner>} Usage breakdown by endpoint
     */
    constructor(userId, plan, totalUsage, totalErrors, endpointBreakdown) { 
        
        V0UsageGet200Response.initialize(this, userId, plan, totalUsage, totalErrors, endpointBreakdown);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, userId, plan, totalUsage, totalErrors, endpointBreakdown) { 
        obj['user_id'] = userId;
        obj['plan'] = plan;
        obj['totalUsage'] = totalUsage;
        obj['totalErrors'] = totalErrors;
        obj['endpointBreakdown'] = endpointBreakdown;
    }

    /**
     * Constructs a <code>V0UsageGet200Response</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/V0UsageGet200Response} obj Optional instance to populate.
     * @return {module:model/V0UsageGet200Response} The populated <code>V0UsageGet200Response</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new V0UsageGet200Response();

            if (data.hasOwnProperty('user_id')) {
                obj['user_id'] = ApiClient.convertToType(data['user_id'], 'String');
            }
            if (data.hasOwnProperty('plan')) {
                obj['plan'] = ApiClient.convertToType(data['plan'], 'String');
            }
            if (data.hasOwnProperty('totalUsage')) {
                obj['totalUsage'] = ApiClient.convertToType(data['totalUsage'], 'Number');
            }
            if (data.hasOwnProperty('totalErrors')) {
                obj['totalErrors'] = ApiClient.convertToType(data['totalErrors'], 'Number');
            }
            if (data.hasOwnProperty('endpointBreakdown')) {
                obj['endpointBreakdown'] = ApiClient.convertToType(data['endpointBreakdown'], [V0UsageGet200ResponseEndpointBreakdownInner]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>V0UsageGet200Response</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>V0UsageGet200Response</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of V0UsageGet200Response.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['user_id'] && !(typeof data['user_id'] === 'string' || data['user_id'] instanceof String)) {
            throw new Error("Expected the field `user_id` to be a primitive type in the JSON string but got " + data['user_id']);
        }
        // ensure the json data is a string
        if (data['plan'] && !(typeof data['plan'] === 'string' || data['plan'] instanceof String)) {
            throw new Error("Expected the field `plan` to be a primitive type in the JSON string but got " + data['plan']);
        }
        if (data['endpointBreakdown']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['endpointBreakdown'])) {
                throw new Error("Expected the field `endpointBreakdown` to be an array in the JSON data but got " + data['endpointBreakdown']);
            }
            // validate the optional field `endpointBreakdown` (array)
            for (const item of data['endpointBreakdown']) {
                V0UsageGet200ResponseEndpointBreakdownInner.validateJSON(item);
            };
        }

        return true;
    }


}

V0UsageGet200Response.RequiredProperties = ["user_id", "plan", "totalUsage", "totalErrors", "endpointBreakdown"];

/**
 * User identifier
 * @member {String} user_id
 */
V0UsageGet200Response.prototype['user_id'] = undefined;

/**
 * Current subscription plan
 * @member {module:model/V0UsageGet200Response.PlanEnum} plan
 */
V0UsageGet200Response.prototype['plan'] = undefined;

/**
 * Total number of API calls across all endpoints
 * @member {Number} totalUsage
 */
V0UsageGet200Response.prototype['totalUsage'] = undefined;

/**
 * Total number of errors across all endpoints
 * @member {Number} totalErrors
 */
V0UsageGet200Response.prototype['totalErrors'] = undefined;

/**
 * Usage breakdown by endpoint
 * @member {Array.<module:model/V0UsageGet200ResponseEndpointBreakdownInner>} endpointBreakdown
 */
V0UsageGet200Response.prototype['endpointBreakdown'] = undefined;





/**
 * Allowed values for the <code>plan</code> property.
 * @enum {String}
 * @readonly
 */
V0UsageGet200Response['PlanEnum'] = {

    /**
     * value: "free"
     * @const
     */
    "free": "free",

    /**
     * value: "starter"
     * @const
     */
    "starter": "starter",

    /**
     * value: "pro"
     * @const
     */
    "pro": "pro",

    /**
     * value: "growth"
     * @const
     */
    "growth": "growth"
};



export default V0UsageGet200Response;

